import { StyleSheet } from "react-native-web";

export const stylesUsed = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    button: {
        padding: 20,
        backgroundColor: 'blue',
    },
    headingText: {
        fontSize: 36,
    },
    userText: {
        textAlign: 'center',
        fontSize: 28,
        padding: 15,
        borderWidth: 1,
        borderColor: 'black',
        marginHorizontal: 16,
    },
    textInputContainer: {
        alignItems:'center'
    }
  });